/** Current value — safe to read anywhere; SSR (no window) returns false. */
export declare function getForceThemeTransition(): boolean;
/** Persist a new value and notify subscribers. */
export declare function setForceThemeTransition(value: boolean): void;
/** Reactive hook for React components (Settings page + the toggle wrappers). */
export declare function useForceThemeTransition(): boolean;
//# sourceMappingURL=force-theme-transition.d.ts.map