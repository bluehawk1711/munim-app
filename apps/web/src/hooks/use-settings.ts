"use client"

/**
 * Web settings hooks — thin re-exports of the shared @munim/query hooks.
 */
import { useSettings, useUpdateSettings } from "@munim/query"

export { useSettings, useUpdateSettings }
export type { SettingsDto as ShopSettings } from "@munim/core"
