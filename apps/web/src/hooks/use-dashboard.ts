"use client"

/**
 * Web dashboard + report hooks — thin re-exports of the shared @munim/query
 * hooks (one API-calling layer across web, desktop and mobile).
 */
export { useDashboard, useReport } from "@munim/query"
